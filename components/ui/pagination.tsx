import Link from "next/link";
import type { Meta } from "@/services/admin.service";
export function Pagination({ meta, query }: { meta: Meta; query: Record<string,string|undefined> }) { if(meta.pages<=1)return null; const href=(page:number)=>`?${new URLSearchParams({...Object.fromEntries(Object.entries(query).filter(([,v])=>v)),page:String(page)} as Record<string,string>)}`; return <div className="flex items-center justify-between border-t border-gray-100 px-4 py-3 text-xs text-gray-500"><span>{meta.total} total · Page {meta.page} of {meta.pages}</span><div className="flex gap-2"><Link aria-disabled={meta.page<=1} className={`btn btn-secondary min-h-8 px-3 ${meta.page<=1?"pointer-events-none opacity-40":""}`} href={href(meta.page-1)}>Previous</Link><Link aria-disabled={meta.page>=meta.pages} className={`btn btn-secondary min-h-8 px-3 ${meta.page>=meta.pages?"pointer-events-none opacity-40":""}`} href={href(meta.page+1)}>Next</Link></div></div>; }
