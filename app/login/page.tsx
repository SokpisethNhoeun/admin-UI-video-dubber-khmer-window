import type { Metadata } from "next";
import { LoginForm } from "@/components/auth/login-form";
export const metadata: Metadata = { title: "Sign in" };
export default async function LoginPage({ searchParams }: { searchParams: Promise<{expired?:string}> }) { const {expired}=await searchParams; return <main className="grid min-h-screen place-items-center bg-background px-4"><div className="w-full max-w-[420px]"><div className="mb-7 text-center"><span className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-[#166b4f] text-base font-bold text-white shadow-lg shadow-emerald-900/10">DV</span><h1 className="mt-5 text-2xl font-semibold tracking-tight">Welcome back</h1><p className="mt-1.5 text-sm text-gray-500">Manage licenses, payments, and customers.</p></div><div className="card p-6 sm:p-8">{expired&&<p className="mb-4 rounded-lg bg-amber-50 p-3 text-xs text-amber-800">Your session expired. Please sign in again.</p>}<LoginForm/></div><p className="mt-5 text-center text-[11px] text-gray-400">Protected administration area · JWT secured</p></div></main>; }
